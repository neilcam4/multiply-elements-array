function multiplyAll(input, multiplier){
    
        var newArray = 
        input.map(function(item){
            return item * multiplier
        })
        return newArray
}

    // console.log(result)
    

